%
\if\FHVmode\paper
	% for multicols sep
	\setlength{\columnsep}{7mm}
	% align environment
	\setlength{\abovedisplayskip}{0pt}
	\setlength{\belowdisplayskip}{0pt}
	\setlength{\abovedisplayshortskip}{0pt}
	\setlength{\belowdisplayshortskip}{0pt}
	\setlength{\jot}{0pt}% tweak
	\SetAlFnt{\scriptsize}
	%
	\setlist[itemize]{leftmargin=*}
	\setlist[enumerate]{leftmargin =*}
	\setlist[description]{style=unboxed,leftmargin=0cm}
	\setkomafont{section}{\normalsize}
	\setlist{itemsep=1pt,topsep=2pt,parsep=0pt}
	% Bilder Abstand
	\setlength{\textfloatsep}{0pt}
	\setlength{\floatsep}{0pt}
	\setlength{\intextsep}{9pt}
	% Mathe Gleichung Abstand
	\setlength\abovedisplayshortskip{0pt}
	\setlength\belowdisplayshortskip{0pt}
	\setlength\abovedisplayskip{0pt}
	\setlength\belowdisplayskip{0pt}
	% Abstand Caption
	\setlength{\abovecaptionskip}{2pt}
	\setlength{\belowcaptionskip}{0pt}
	\captionsetup[subfigure]{aboveskip=2pt}
	\captionsetup[subfigure]{belowskip=0pt}
	\setlength{\parskip}{0pt}
	%
	\addto\captionsngerman{
		\renewcommand{\figurename}{Abb.}
		\renewcommand{\tablename}{Tab.}
	}
	\setkomafont{captionlabel}{\normalsize\bfseries} 
	\renewcaptionname{ngerman}{\figurename}{Abb.}% nach Bedarf
	\renewcaptionname{ngerman}\figureautorefname{Abb.}
	\renewcaptionname{ngerman}{\tablename}{Tab.}% nach Bedarf
	\renewcaptionname{ngerman}\tableautorefname{Tab.}
	%
	\setlength{\parindent}{6.3mm} 
	\setlength{\footnotemargin}{0pt} %fussnoten nicht einruecken!
	%
	\renewcommand{\arraystretch}{0.8}%tabelle vertikaler abstand verringern
	% Bib. space settings
	\setlength{\bibhang}{6.3mm} 
%	\setlength\bibitemsep{-6pt}
	\setlength\bibnamesep{0pt}
	% Section space settings
	\RedeclareSectionCommand[
	beforeskip=9pt,
	afterskip=9pt]{section}
	\RedeclareSectionCommand[
	beforeskip=9pt,
	afterskip=9pt]{subsection}
	\RedeclareSectionCommand[%
	beforeskip=9pt,%
	afterskip=-2pt%
	]{paragraph}
	%
\else
	%\setlength\bibitemsep{1.5\itemsep}
	\setlength\bibparsep{1.5\itemsep}
	%\setlength\bibinitsep{1.5\itemsep}
	\setlength\bibnamesep{1.5\itemsep}
\fi
